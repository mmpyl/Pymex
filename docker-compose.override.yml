# Docker Compose override para desarrollo local
# Copiar a docker-compose.override.yml y personalizar

services:
  postgres:
    environment:
      POSTGRES_PASSWORD: admin123_secure_local_2024

  backend:
    environment:
      DB_PASSWORD: admin123_secure_local_2024
      JWT_SECRET: jwt_secret_super_seguro_para_desarrollo_local_2024_muy_largo_64_chars_minimo_este_es_ejemplo
      NODE_ENV: development

  ml_service:
    environment:
      DB_PASSWORD: admin123_secure_local_2024

  facturacion_service:
    environment:
      DB_PASSWORD: admin123_secure_local_2024
      APP_ENV: development
      SUNAT_RUC: "20123456789"
      SUNAT_USER: MODDATOS
      SUNAT_PASSWORD: moddatos
